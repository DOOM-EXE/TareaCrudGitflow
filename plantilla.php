<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Breaking Bad CRUD</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilos generales */
        body {
            background-size: cover;
            color: white;
            font-family: 'Arial', sans-serif;
        }
        .container {
            color: white;
            max-width: 900px;
            margin: auto;
            padding: 20px;
            background: rgba(0, 0, 0, 0.85);
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.5);
        }
        h1 {
            color: #53A548;
            text-align: center;
            font-weight: bold;  
            text-shadow: 2px 2px 4px rgba(0, 255, 0, 0.6);
        }
        /* Estilos para botones */
        .btn-success { background: #53A548; border: none; }
        .btn-warning { background: #F1C40F; border: none; }
        .btn-danger { background: #C0392B; border: none; }
        .btn-primary { background: #2980B9; border: none; }
        /* Tabla personalizada */
        .table {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }
        .table th {
            background: #53A548;
            color: white;
            text-align: center;
        }
        .table td {
            text-align: center;
        }
        /* Estilos para imágenes */
        .profile-img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 2px solid #53A548;
        }
    </style>
</head>
<body>

    <div class="container mt-4">
        <div class="text-center mb-4">
            <img src="img/breakinglogo.png" width="250" alt="Breaking Bad Logo">
        </div>

        <!-- Contenido dinámico -->

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
